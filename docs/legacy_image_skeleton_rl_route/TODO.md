# 待办事项

## 优先级 HIGH

- [x] 完成最小程序：输入字形图片 → 二值化 → 骨架提取 → 初始轨迹 → 平滑轨迹 → 输出轨迹点csv
- [x] 实现强化学习轨迹优化模块（DDPG）
- [x] 设计结合形状误差和平滑性约束的奖励函数
- [x] 升级骨架提取为 skimage.morphology.thin（交叉点噪点降低 20 倍）
- [x] 重写笔画提取算法（简化图 + 端点方向配对）
- [x] 实现逐笔画 B-spline 平滑 + 抬笔分隔（nan,nan）
- [ ] 完成 5+ 个字形样本的仿真验证
- [ ] 完成三组对比实验（骨架基线 / B样条平滑 / RL 优化）
- [ ] 画出完整流程图：原图 → 骨架 → 轨迹 → 优化轨迹
- [ ] 找 15+ 篇参考文献（近三年为主）

## 下一轮算法任务（2026-05-17）

- [ ] 建立并固定小测试集划分：调参集 `yi, san, shi, kou, tian, zhong, chuan, zhi, yong, fu, mu, ming`；保留测试集 `ri, ren, da, shan, lin, hao, xiu, guo, hui, pin, xin, shui, xiao`
- [ ] 调参阶段只看调参集，保留测试集只用于阶段性泛化检查，避免针对单字过拟合
- [ ] 将 `code/stroke.py` 中的 junction 贪心配对升级为候选路径生成 + 全局代价评分
- [ ] 在评分函数中加入方向连续性、回绕惩罚、骨架覆盖率、闭合结构保护、笔画数先验、短毛刺惩罚、部件边界约束
- [ ] 增加每个字的结构化日志：预测笔画数、骨架覆盖率、异常回绕数量、短分支删除数量、闭合结构是否保留
- [ ] 以“川”作为简单字底线验收，确保复杂字修正不会导致简单字主体笔画缺失
- [ ] 以“中/口/田/福”作为闭合结构验收，确保框线不被拆散、不变成异常大弧线

### 本轮阶段结论
- [x] global 路径已可通过安全门局部接入：`chuan`、`kou` 使用 global；其余复杂字暂回退 legacy。
- [x] 已增加结构化日志：字名、预测笔画数、skeleton 像素数、endpoints、junction-px、method、fallback reason。
- [x] 已修复 global 候选重叠导致的 `tian/zhong` 过覆盖问题。
- [ ] 下一步继续修 `tian/zhong` 的闭合结构候选评分，目标是减少 global 的覆盖过量和大弧线绕行。

## 优先级 LOW

- [ ] 将优化轨迹映射到机械臂工作空间
- [ ] 实物验证实验（末端夹爪夹持笔完成简单字形轨迹跟踪）
- [ ] 撰写论文正文
- [ ] 整理参考文献格式
